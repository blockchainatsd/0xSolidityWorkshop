/**
 * Viem Examples Index
 * 
 * This directory contains example code for using viem with the TipWall contract.
 * 
 * Files:
 * - readContract.ts   - Reading state from the contract (totalTipped, getTip, etc.)
 * - writeContract.ts  - Sending transactions (tip, withdraw)
 * - watchEvents.ts    - Subscribing to real-time events (NewTip, Withdraw)
 */

export * from './readContract';
export * from './writeContract';
export * from './watchEvents';
